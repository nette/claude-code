{
	"description": "Validate Latte templates and NEON files",
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write",
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/hooks/lint-latte.sh"
					},
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/hooks/lint-neon.sh"
					}
				]
			}
		]
	}
}
